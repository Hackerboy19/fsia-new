/* ============================================================
   FOREVER STAR INDIA — CMS CONFIG
   Paste your two Supabase values below (one time).
   Find them in Supabase: Project Settings → API
   ============================================================ */
window.FSIA_CONFIG = {
  // Looks like: https://abcdefgh.supabase.co
  SUPABASE_URL: "PASTE_YOUR_PROJECT_URL_HERE",

  // The "anon / public" key. Safe to expose — your data is
  // protected by the security rules in supabase-setup.sql.
  SUPABASE_ANON_KEY: "PASTE_YOUR_ANON_PUBLIC_KEY_HERE"
};
